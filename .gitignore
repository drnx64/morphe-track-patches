# Local environment variables
.env

# Python files
__pycache__/
*.pyc
*.pyo
*.pyd

# Raw data caching (keep parsed output and state in git, but ignore raw large downloads if preferred,
# though we can keep some logs/state so they persist. Let's ignore raw downloads)
data/raw/bundles/

# GitHub Pages website files (deployed to gh-pages branch)
/index.html
/changelog.html
/assets/
/sw.js
/feed.xml
/ARCHITECTURE.md

# Generated data files (deployed to gh-pages branch)
/data/core.json
/data/stats.json
/data/changes.json
/data/bundles.json
/data/changelog.json

# OS files
.DS_Store
Thumbs.db
