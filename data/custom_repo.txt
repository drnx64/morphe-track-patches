# Custom repositories — one per line, format: owner/repo
# These repos will be fetched in addition to the default ones.
# Use "gl:owner/repo" prefix for GitLab repos.
# Examples:
#   someuser/some-patches
#   gl:someuser/some-gitlab-patches


