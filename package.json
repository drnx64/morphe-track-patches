{
  "name": "morphe-tracker",
  "private": true,
  "version": "5.0.0",
  "type": "module",
  "description": "Automated patch tracker for Morphe application patches",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "node scripts/build.js",
    "preview": "npx serve docs -p 3000",
    "pull-data": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/pull-data.ps1"
  },
  "dependencies": {},
  "devDependencies": {}
}
